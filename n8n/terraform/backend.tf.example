# Example backend configuration for GCS
# To use remote backend:
# 1. Copy this file to backend.tf
# 2. Update the bucket name and prefix for your environment
# 3. Run: terraform init -migrate-state

terraform {
  backend "gcs" {
    bucket = "your-project-tfstate"  # Replace with your GCS bucket name
    prefix = "n8n/terraform"          # Path within the bucket for state files
  }
}